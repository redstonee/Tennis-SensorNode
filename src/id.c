#include <stdint.h>

// These constants are used to define the sensor ID and network ID for the Lora module
// Will be patched by a script when flashing the firmware
const uint8_t sensorID = 0; // Sensor ID
const uint8_t netID = 11;   // Network ID of the nodes